<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TradeFutNight"
        xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid" x:Class="TradeFutNight.temp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:TestViewModel />
    </Window.DataContext>
    <Grid>
        <Button Content="Button" HorizontalAlignment="Left" Margin="35,31,0,0" VerticalAlignment="Top" Width="50" Command="{Binding SaveCommand}" />
        <dxg:GridControl AutoGenerateColumns="AddNew" EnableSmartColumnsGeneration="True" HorizontalAlignment="Left" Margin="161,56,0,0" VerticalAlignment="Top" Width="516" Height="247" ItemsSource="{Binding Orders}">
            <dxg:GridControl.View>
                <dxg:TableView x:Name="gridMain" ShowTotalSummary="True" />
            </dxg:GridControl.View>
        </dxg:GridControl>
    </Grid>
</Window>